{
  "name": "padel-gpt",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "PADEL//NOVA — professional browser-based 3D padel game deployed on cscarpatic/Padel-GPT.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "lint": "node --check src/main.js && node --check src/scoring.js && node --check src/physics.js && node --check src/athlete.js && node --check src/gameplay.js",
    "test": "node --test tests/*.test.js",
    "check": "npm run lint && npm run test && npm run build"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "vite": "8.2.1"
  }
}
